apiVersion: apps/v1
kind: Deployment
metadata:
  name: frontend
  namespace: crud-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: frontend
  template:
    metadata:
      labels:
        app: frontend
    spec:
      containers:
        - name: frontend
          image: 360025768948.dkr.ecr.us-east-1.amazonaws.com/crud-app-frontend:11
          ports:
            - containerPort: 5000
---
apiVersion: v1
kind: Service
metadata:
  name: frontend-svc
  namespace: crud-app
spec:
  selector:
    app: frontend
  ports:
    - port: 80
      targetPort: 5000
  type: LoadBalancer
